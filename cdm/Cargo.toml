[package]
name = "cdm"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 2
debug = false

[dependencies]
bitmask-enum = "2.2.5"
critical-section = { version = "1.2.0", features = ["restore-state-u16"] }

[features]
panic-halt = []
