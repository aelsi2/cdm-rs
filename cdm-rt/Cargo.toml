[package]
name = "cdm-rt"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[profile.dev]
opt-level = 2
debug = false

[dependencies]
cdm = { path = "../cdm" }
cdm-macros = { path = "../cdm-macros" }

[features]
interrupts = []
